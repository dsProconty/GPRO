{
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma db push && node prisma/seed.js && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/cron/recordatorios",
      "schedule": "0 8 * * *"
    }
  ]
}
